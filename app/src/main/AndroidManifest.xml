<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.j329.carefreesleepzzz">

    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:launchMode="standard"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Mainmenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".Sleepchecker"></activity>
        <activity android:name=".notWakeup"></activity>
        <activity android:name=".Configs"></activity>

        <service android:name=".Pleaseworksomething" ></service>

        <receiver android:name=".AlarmBroadcastReceiver" android:process=":remote" />
    </application>
    <!--android:theme="@style/Theme.AppCompat.Light.NoActionBar"-->

</manifest>